services:
  executron:
    image: twirapp/executron:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp:/tmp
    restart: unless-stopped

networks:
  twir:
    external: true
  cloudflared:
    external: true