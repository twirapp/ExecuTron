services:
  executron:
    image: twirapp/executron:latest
    volumes:
      - /var/run/docker.sock:ro:/var/run/docker.sock
    restart: unless-stopped

networks:
  twir:
    external: true
  cloudflared:
    external: true